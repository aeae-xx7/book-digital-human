I have revised the plan to focus on a **Global Navigation System**, **One-Click Access**, and the **Luxury Aesthetic**.

**1. Global Navigation & Layout (`src/app/layout.tsx`** **&** **`src/components/NavBar.tsx`)**

* **Action:** Create a global `layout.tsx` (currently missing) to wrap the entire application.

* **Navigation Bar:**

  * **Logo/Home:** "纸上相遇" (Serif, elegant).

  * **Module Links:** Direct links to the 3 core experiences ("若你在书中", "若书遇见你", "留下一念") in the header, allowing users to understand the product scope immediately.

  * **My Bookshelf:** A prominent button to access saved books.

**2. "My Bookshelf" Drawer (`src/components/BookshelfDrawer.tsx`)**

* **Action:** Implement a **Slide-over Drawer** (or Modal) for the Bookshelf.

* **Benefit:** Users can access their library from *anywhere* (Home, Book Page, Experience) without leaving their current context.

* **One-Click Experience:** Clicking a book in the drawer will directly resume the last session or open the book detail page.

**3. Homepage Redesign (`src/app/page.tsx`)**

* **Luxury Aesthetic:**

  * **Background:** Warm off-white (`#fdfbf7`) to mimic high-quality paper.

  * **Typography:** Elegant Serif fonts for headings.

  * **Layout:** Center the Search bar, but display the **3 Experience Modules** as elegant cards *below* the search, explaining what they are. This "Shortens the Path" to understanding the product.

* **Search:** Minimalist, high-end input design.

**4. Book Detail Page (`src/app/book/page.tsx`)**

* **Optimization:** Move the **Experience Entry Section** to the top (Hero section).

* **Style:** Match the new luxury theme.

**Implementation Sequence:**

1. Create `src/app/globals.css` (for base styles/fonts) and `src/app/layout.tsx`.
2. Create `src/components/NavBar.tsx` and `src/components/BookshelfDrawer.tsx`.
3. Refactor `src/app/page.tsx` (Homepage) with the new design and module showcase.
4. Refactor `src/app/book/page.tsx` (Detail Page) for better hierarchy.
5. Refactor `src/components/ExperienceEntrySection.tsx` to match the luxury style.

